version: "3.8"

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: your-registry/chatapp-backend:latest
    restart: always
    environment:
      - NODE_ENV=production
      - PORT=4600
    env_file:
      - ./prod.env
    ports:
      - "4600:4600"
    networks:
      - webnet
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:4600/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: your-registry/chatapp-frontend:latest
    restart: always
    networks:
      - webnet
    ports:
      - "81:80"
    depends_on:
      - backend

  proxy:
    image: nginx:stable-alpine
    restart: always
    volumes:
      # Updated proxy config
      - ./nginx/proxy.conf:/etc/nginx/conf.d/default.conf:ro

      # Updated SSL (self-signed certificate directory)
      - /etc/nginx/ssl:/etc/nginx/ssl:ro

    ports:
      - "80:80"
      - "443:443"
    networks:
      - webnet
    depends_on:
      - frontend
      - backend

networks:
  webnet:
    driver: bridge
